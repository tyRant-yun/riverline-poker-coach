# R8-04 Solver Sizing Calibration handoff

```yaml
contract_version: handoff/v1
task_id: R8-04
thread_id: /root/r8_04_solver_sizing
branch: codex/r8-04-solver-sizing
base_commit: 3d2081b24bf7c74bb617c6e59b62c5735e48f37d
head_commit: ec1bb0b7fabdf4695acbe5b9f2b2f735b643612c
status: completed
scope:
  goal: Calibrate Fast Solver legal sizing candidates, response mix, common-sample EV uncertainty, and robust recommendation evidence without weakening amount, privacy, budget, oracle, or non-GTO contracts.
  included:
    - Generate integer, deduplicated 33/50/66/75/100 percent-pot candidates inside each LegalActionV1 interval while preserving legal min/max, explicit overbet, and jam candidates.
    - Preserve BET BY and RAISE TO semantics, including Hero incremental cost, opponent stack-capped completion, continuation pot, and LegalActionV1.accepts validation.
    - Calibrate bounded sizing pressure with SPR, position, public range concentration, and correct multiway aggregate fold/call/raise probabilities.
    - Expose pot percentage, jam/sizing class, delta EV, candidate CI availability, common-sample delta CI, sizing robustness, conservative close tie-break, and machine-readable reason codes.
    - Feed validated solver margin-CI overlap and sizing robustness into R8-03 reconciliation without adding a final arbitration field or exposing untrusted solver payloads.
    - Freeze check/call/fold, standard sizing, overbet, jam, RAISE TO, SPR, position, range concentration, multiway, false-positive extreme, robust jam, close/CI, privacy, budget, determinism, and oracle behavior in focused tests.
  excluded:
    - Frontend, Range core, FormulaAdvisor, rule/amount authority, persistence, ledger, dependencies, GTO/Nash/CFR claims, population/player models, push, main integration, full backend, live PostgreSQL, and final product review.
changed_files:
  - backend/poker_coach/simulator/decision_reconciliation.py
  - backend/poker_coach/simulator/fast_solver.py
  - backend/tests/test_decision_reconciliation.py
  - backend/tests/test_fast_solver.py
  - backend/tests/test_solver_sizing_calibration.py
commits:
  - sha: ec1bb0b7fabdf4695acbe5b9f2b2f735b643612c
    subject: "feat(solver): calibrate sizing recommendations"
quality_gates:
  - command: 'py -3.13 -m pytest -o addopts="" backend/tests/test_solver_sizing_calibration.py -q'
    result: "Expected RED before implementation: 7 failed. Missing standard pot candidates, RAISE TO generated amounts, multiway response calibration, jam labels, delta EV/CI, robustness, and insufficient-sample honesty."
    measured: true
  - command: 'py -3.13 -m pytest -o addopts="" backend/tests/test_decision_reconciliation.py -q'
    result: "Expected reconciliation RED before wiring: 2 failed because sizingRobustness and real margin-CI overlap were absent."
    measured: true
  - command: 'py -3.13 -m pytest -o addopts="" backend/tests/test_fast_solver.py backend/tests/test_solver_sizing_calibration.py backend/tests/test_decision_reconciliation.py -q -s'
    result: "PASS at delivery head: 35 passed in 2.91s. Conditional joint fixture 48.08%/51.32%/0.60%; river equity oracle MAE 0.000000pp; 25-run 6-player/full-1326 standard benchmark p50 33.660ms and p95 35.877ms (targets p50 <150ms, p95 <300ms)."
    measured: true
  - command: 'py -3.13 -m pytest -o addopts="" backend/tests/test_continuous_table_api.py::test_table_fast_solver_is_independent_and_rejects_a_stale_decision backend/tests/test_decision_reconciliation.py -q; py -3.13 -m compileall -q backend/poker_coach/simulator; git diff --check'
    result: "PASS before delivery commit: 11 passed in 1.79s; compileall exit 0 with no output; diff check exit 0 with only expected LF-to-CRLF notices."
    measured: true
  - command: 'git status --short; git diff --check; py -3.13 -m compileall -q backend/poker_coach/simulator; py -3.13 -m pytest -o addopts="" backend/tests/test_continuous_table_api.py::test_table_fast_solver_is_independent_and_rejects_a_stale_decision -q'
    result: "PASS immediately before delivery commit: only five intended files changed; diff check and compileall exit 0; direct API node 1 passed in 0.82s."
    measured: true
  - command: 'py -3.13 -m pytest backend -q'
    result: "Not run by task contract; full backend remains a Controller/stage gate."
    measured: false
  - command: 'frontend test/build/E2E and live PostgreSQL'
    result: "Not run because frontend, release, and live database gates are excluded from R8-04."
    measured: false
artifacts:
  - path: backend/tests/test_solver_sizing_calibration.py
    description: Frozen R8-04 product sizing spots and directionality/robustness counterexamples.
  - path: backend/poker_coach/simulator/fast_solver.py
    description: Additive Fast Solver candidate and result evidence for the Action Ladder and reconciliation consumers.
risks:
  - "Response probabilities remain a bounded public heuristic based on sizing, SPR, position, continuation-range concentration, street, and multiway aggregation; they are not calibrated population data, learned range strength, GTO, Nash, CFR, or player/profile output."
  - "EV confidence intervals and sizing robustness quantify common-showdown-sample uncertainty only. They deliberately do not quantify error in the response heuristic; exact river output means deterministic within this model, not strategically exact poker play."
  - "The conservative close tie-break is deterministic and visible through delta EV/reason codes, but downstream UI must continue showing the rule baseline and simulation estimate separately."
  - "Only focused backend tests were run; full backend/frontend/E2E/live-PostgreSQL gates remain unmeasured by this task."
decisions_needed: []
dependencies_unlocked:
  - R8-05
recommended_next:
  - task_id: R8-05
    goal: Run the integrated real-hand product evaluation, four-viewport UX gate, stale-response/privacy checks, and the single final whole-plan review.
    depends_on:
      - R8-01
      - R8-02
      - R8-03
      - R8-04
```
