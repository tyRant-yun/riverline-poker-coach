```yaml
contract_version: handoff/v1
task_id: R5-06
thread_id: 019ff500-f1ba-7af2-9663-5c4aa1826bf6
branch: codex/r5-06-fast-solver
base_commit: 6524fd638f8797d766387b38770c10b75f48aa38
head_commit: 2ee32cbc9b5dfc2a2e80161e6c1837409f3e2714
status: completed
scope:
  goal: Add a bounded, deterministic Fast EV Solver L1 that augments but never blocks the table's L0 Formula Advisor.
  included:
    - Read-only FastSolver Monte Carlo equity and action-level approximate-EV service with deterministic seed, 75 ms soft/150 ms hard budgets, legal-action-only candidates, and safe provenance.
    - Separate table solver endpoint with active decision fingerprint conflict rejection and no table-lock-held sampling.
    - ContinuousTable Solver panel/client DTO with L0/L1 parallel loading, degraded/unavailable rendering, stale-response suppression, and next-decision clearing.
  excluded:
    - Rule/deal/PHH/settlement/rebuy/showdown changes; action execution; GTO/Nash claims; solver sidecar; Redis/jobs; persistence; legacy pages; and licenses.
changed_files:
  - backend/poker_coach/api/app.py
  - backend/poker_coach/simulator/__init__.py
  - backend/poker_coach/simulator/continuous_table.py
  - backend/poker_coach/simulator/fast_solver.py
  - backend/tests/test_continuous_table_api.py
  - backend/tests/test_fast_solver.py
  - frontend/features/table/ContinuousTablePage.test.tsx
  - frontend/features/table/ContinuousTablePage.tsx
  - frontend/lib/api/client.ts
  - frontend/types/api.ts
commits:
  - sha: 2ee32cbc9b5dfc2a2e80161e6c1837409f3e2714
    subject: "feat(table): add bounded fast EV solver"
quality_gates:
  - command: py -3.13 -m pytest backend/tests/test_fast_solver.py backend/tests/test_continuous_table_api.py -q
    result: initial red: ImportError for missing FastSolver
    measured: true
  - command: npx vitest run features/table/ContinuousTablePage.test.tsx
    result: initial red attempt blocked before collection because frontend node_modules lacked vitest/config and @vitejs/plugin-react; after npm ci, final result 1 file / 6 tests passed
    measured: true
  - command: py -3.13 -m pytest backend/tests/test_fast_solver.py backend/tests/test_continuous_table_api.py backend/tests/test_formula_advisor.py -q
    result: 19 passed
    measured: true
  - command: py -3.13 -m compileall -q backend/poker_coach/simulator backend/poker_coach/api
    result: exit 0
    measured: true
  - command: npx tsc --noEmit
    result: exit 0
    measured: true
  - command: git diff --check
    result: exit 0 before implementation commit and after final focused verification
    measured: true
  - command: py -3.13 -c "... FastSolver default 120-iteration multiway-flop smoke ..."
    result: status=ready; iterations=120; elapsed_ms=4.263; service_elapsed_us=4241
    measured: true
artifacts:
  - path: backend/poker_coach/simulator/fast_solver.py
    description: Versioned Fast EV Solver L1 result contract and deterministic uniform-opponent Monte Carlo evaluator.
  - path: backend/poker_coach/simulator/continuous_table.py
    description: Read-only, stale-safe table solver snapshot endpoint service.
  - path: frontend/features/table/ContinuousTablePage.tsx
    description: Solver panel that renders ready/degraded/unavailable L1 separately from immediate L0 insights.
risks:
  - L1 is a coarse uniform-opponent, one-street showdown approximation; it does not use a combo distribution, fold equity, future betting, GTO/Nash, range-aware, or opponent-truth claim.
  - The endpoint output intentionally omits hero cards, opponent samples, opponent cards, and future runouts; the result source documents the uniform model.
  - L1 runs synchronously within its bounded endpoint; a slow client request receives degraded/unavailable while L0 remains a separate request. No API end-to-end load benchmark was run.
  - npm ci reported 3 high-severity dependency audit findings and Node 24.18.0 versus package engine 24.15.0; neither was modified because they are outside this task scope.
decisions_needed: []
dependencies_unlocked:
  - focused solver/privacy review
  - MVP release gate
recommended_next:
  - task_id: release-gate
    goal: Run the planned focused solver/private-information review, then include R5-06 in the release gate.
    depends_on:
      - R5-06
```

Baseline note: the task was requested at `b621148452272990557baae8c9667d6f00e678fc`.
After the one permitted fetch, `origin/codex/simulator-rebuild` resolved to
`6524fd638f8797d766387b38770c10b75f48aa38`, which contains that commit and
differs only by the baseline-pinning `docs/orchestration/ledger.md` commit.
The task branch was rebuilt from that equivalent elevated baseline before code
changes.

Formula note: every trial deals Hero's supplied private cards, public board,
uniform unknown opponent cards, and runout from one 52-card deck without
overlap. Equity is Hero's showdown share. Fold is zero incremental chips;
check/call/bet/raise candidates use current-pot, legal minimum amount, and
one-street showdown approximate EV. Bet/raise values preserve the frozen
`by`/`to` semantics and are explicitly limited in the result provenance.
