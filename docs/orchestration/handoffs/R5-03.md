# R5-03 Local Experience handoff

```yaml
contract_version: handoff/v1
task_id: R5-03
thread_id: 019ff53a-fe87-7353-b7cc-ac3288f0553e
branch: codex/r5-03-local-experience
base_commit: a85784c50f10a43d3058f0ba6fdab52d68f960fc
head_commit: 07147e95825b99bfe2ea2eadf77d8c79e6f1b667
status: completed
scope:
  goal: Make the default local Riverline experience start reliably with SQLite and no Redis despite external-service values in .env, while restoring meaningful focused browser coverage.
  included:
    - Default child-process-only SQLite/no-Redis environment masking, with explicit -UseExternalServices opt-in.
    - Port checks, readiness checks, hidden child processes, timestamped logs, actionable failure cleanup, and optional alternate API/Web ports.
    - Direct PowerShell launch-contract regression and minimal README/experience-guide instructions.
    - Real local browser smoke plus shell and six-seat multiple-Hero-action E2E coverage.
  excluded:
    - Product backend/frontend behavior changes, bot/session/rules/amount/privacy changes, license work, central ledger, push, and main merge.
    - Any change to frontend/next-env.d.ts.
changed_files:
  - README.md
  - docs/mvp-experience-guide.md
  - frontend/e2e/local-experience.spec.ts
  - frontend/e2e/multiseat-scenario.spec.ts
  - frontend/e2e/mvp-shell.spec.ts
  - scripts/run-local.ps1
  - scripts/test-run-local-contract.ps1
commits:
  - sha: 07147e95825b99bfe2ea2eadf77d8c79e6f1b667
    subject: feat: make local experience self-contained
quality_gates:
  - command: pwsh -NoProfile -File scripts/test-run-local-contract.ps1
    result: run-local.ps1 contract passed
    measured: true
  - command: .\scripts\run-local.ps1 -ApiPort 8100 -WebPort 3100 with HTTP health, table create, and web checks
    result: Default SQLite/no-Redis mode reached health=ok, created a table with 3 hero actions, served web=200, then the exact two task-started process trees were cleaned.
    measured: true
  - command: isolated default launcher API insights check
    result: insights available=True; advisor=True; beliefs=5; stats=False (honest unavailable state)
    measured: true
  - command: npx playwright test e2e/local-experience.spec.ts e2e/mvp-shell.spec.ts e2e/multiseat-scenario.spec.ts
    result: 3 passed; real local-service smoke verifies online health, table creation, Hero action dock, six seats, and Advisor/Range/Stats/Solver partition.
    measured: true
  - command: npx tsc --noEmit
    result: passed
    measured: true
  - command: npm run build
    result: Next.js production build passed
    measured: true
  - command: git diff --check
    result: exit 0; existing Git CRLF conversion warnings only
    measured: true
artifacts:
  - path: scripts/run-local.ps1
    description: One-command local launcher whose default process environment masks external DB/Redis settings without modifying .env.
  - path: scripts/test-run-local-contract.ps1
    description: Focused static regression for launcher mode, masking, readiness, hidden process, and process-tree cleanup contracts.
  - path: frontend/e2e/local-experience.spec.ts
    description: Real default local-service browser smoke for online health, table creation, Hero action dock, six seats, and honest insight sections.
risks:
  - Default ports 8000/3000 remain intentionally guarded; if another service owns either port, users must stop it or explicitly choose -ApiPort/-WebPort.
  - -UseExternalServices deliberately inherits .env/environment values and will fail with a log-backed diagnostic if the selected PostgreSQL or Redis service is unavailable.
  - The real smoke validates one table creation rather than a long multi-hand soak or live PostgreSQL/Redis workflow.
decisions_needed: []
dependencies_unlocked:
  - Local MVP user validation
recommended_next:
  - task_id: LOCAL-MVP-USER-VALIDATION
    goal: Run the default launcher from the user workspace and collect the first continuous-table feedback session.
    depends_on:
      - R5-03
```
