# R7-05R independent narrow review handoff

```yaml
contract_version: handoff/v1
task_id: R7-05R
thread_id: /root/r7_05r_review
branch: codex/r7-05r
base_commit: 11240329d8a20693a5bdef6e72fd45c2d29e5ae6
head_commit: 5c9023d6b0e3f32e6f897d30cd5810e151d9e0d5
reviewed_head_commit: 5c9023d6b0e3f32e6f897d30cd5810e151d9e0d5
status: completed
scope:
  goal: Independently review the exact R7-05 Fast Solver L1.5 delivery for MVP-blocking P0/P1 range sampling, amount authority, EV/response metrics, budget, oracle, performance, API staleness, actor, and privacy defects.
  included:
    - Review only 11240329d8a20693a5bdef6e72fd45c2d29e5ae6..20c320e37013d703e81444c5878a2849f8b44ec8.
    - Re-review only the original three P1 findings and direct regressions in 20c320e37013d703e81444c5878a2849f8b44ec8..f2b9632bf80545311be4ac30273f02aae87b3481, mapped to review-worktree commit 8e14b204ee9f68eed94fecd8ba81eb04aaca8618.
    - Final-review only the remaining conditional-product P1 and direct regression in f2b9632bf80545311be4ac30273f02aae87b3481..5c9023d6b0e3f32e6f897d30cd5810e151d9e0d5, mapped to review-worktree commit 23b9a65d2d2c96b69245b6fd94de74cc3b5e1e0e.
    - Inspect only the four changed files, direct ObservationV1/LegalActionV1 and public-event Range V2 seams, R7 Solver quality gate, R7-05 handoff, and directly relevant tests.
    - Run only the authorized Fast Solver test file, one related Continuous Table API node, and narrow read-only counterexamples/benchmark diagnostics.
    - Report only P0/P1 findings with minimal counterexamples and repair boundaries.
  excluded:
    - Implementation changes, central ledger edits, full-suite tests, unrelated repository inspection, push, merge, release acceptance, R7-06 CFR/L2, and frontend work.
changed_files:
  - docs/orchestration/handoffs/R7-05R.md
commits: []
quality_gates:
  - command: "git diff --unified=60 11240329d8a20693a5bdef6e72fd45c2d29e5ae6..20c320e37013d703e81444c5878a2849f8b44ec8 -- backend/poker_coach/simulator/fast_solver.py backend/poker_coach/simulator/continuous_table.py backend/tests/test_fast_solver.py backend/tests/test_continuous_table_api.py"
    result: "FAIL review: three P1 findings recorded below; no P0 found."
    measured: true
  - command: "py -3.13 -m pytest -o addopts=\"\" backend/tests/test_fast_solver.py backend/tests/test_continuous_table_api.py::test_table_fast_solver_is_independent_and_rejects_a_stale_decision -q -s"
    result: "PASS: 11 passed in 3.49s; river equity MAE 0.000000pp; test-reported standard p50 88.441ms/p95 92.014ms. Existing assertions do not cover a sampled multiway dead end with valid joint support, opponent call cost after RAISE TO, or probability-sensitive effective range width."
    measured: true
  - command: >-
      py -3.13 -c "from decimal import Decimal as D; from types import SimpleNamespace as N; from poker_coach.simulator import FastSolver,LegalActionV1,ObservationV1; obs=ObservationV1(handId='joint',sequence=1,observerSeat=0,tableSize=3,buttonSeat=0,street='flop',ownHoleCards=('As','Kd'),board=('7c','6d','2h'),pot=300,stacks={0:900,1:900,2:900},streetCommitments={0:0,1:0,2:0},activeSeats=(0,1,2),legalActions=(LegalActionV1(action='check',amountSemantics='none'),)); mk=lambda seat,cs:N(seat_id=seat,available=True,current=N(combos={k:N(probability=D(v)) for k,v in cs.items()},snapshot_id=str(seat)),provenance=N(artifact_fingerprint='x',version='v')); rs={1:mk(1,{'QcQd':'0.5','JhJd':'0.5'}),2:mk(2,{'QcQs':'1'})}; print([(s,FastSolver(iteration_cap=1,clock=lambda:0).solve(obs,decision_fingerprint='joint',seed=s,range_beliefs=rs).source) for s in range(12)])"
    result: "FAIL sampling: seeds 0,2,5,6,10 returned monte_carlo_uniform_opponents even though the compatible assignment seat1=JhJd, seat2=QcQs gives non-empty joint support. Other seeds remained range_weighted_public_beliefs, proving fallback is caused by a sampled dead end rather than genuinely absent joint support."
    measured: true
  - command: >-
      py -3.13 -c "from decimal import Decimal as D; from types import SimpleNamespace as N; from poker_coach.simulator import FastSolver,LegalActionV1,ObservationV1; obs=ObservationV1(handId='ev',sequence=1,observerSeat=0,tableSize=2,buttonSeat=0,street='river',ownHoleCards=('As','Ad'),board=('2c','3d','7h','8s','9c'),pot=300,stacks={0:950,1:900},streetCommitments={0:50,1:100},activeSeats=(0,1),legalActions=(LegalActionV1(action='raise',amountSemantics='to',minAmount=300,maxAmount=300),)); b=N(seat_id=1,available=True,current=N(combos={'KcKd':N(probability=D(1))},snapshot_id='1'),provenance=N(artifact_fingerprint='x',version='v')); c=FastSolver(clock=lambda:0).solve(obs,decision_fingerprint='ev',range_beliefs={1:b}).candidates[0]; print({'equity':str(c.showdown_equity),'hero_incremental_cost':c.incremental_cost,'opponent_call_increment':200,'reported_ev':str(c.approximate_ev_chips),'mix':c.response_mix.to_dict()})"
    result: "FAIL amount semantics: Hero RAISE TO 300 from 50 costs 250, but an opponent already at 100 calls only 200. The implementation uses 250 for both sides; with equity=1 its internal call branch is 550 chips instead of the contract-correct 500 before response mixing."
    measured: true
  - command: >-
      py -3.13 -c "from decimal import Decimal as D; from types import SimpleNamespace as N; from poker_coach.simulator import FastSolver,LegalActionV1,ObservationV1; obs=ObservationV1(handId='width',sequence=1,observerSeat=0,tableSize=2,buttonSeat=0,street='flop',ownHoleCards=('As','Kd'),board=('7c','6d','2h'),pot=300,stacks={0:900,1:900},streetCommitments={0:0,1:0},activeSeats=(0,1),legalActions=(LegalActionV1(action='bet',amountSemantics='by',minAmount=200,maxAmount=200),)); mk=lambda ws:N(seat_id=1,available=True,current=N(combos={k:N(probability=D(v)) for k,v in ws.items()},snapshot_id='1'),provenance=N(artifact_fingerprint='x',version='v')); s=FastSolver(iteration_cap=20,clock=lambda:0); a=s.solve(obs,decision_fingerprint='w',seed=1,range_beliefs={1:mk({'QcQd':'0.5','JhJd':'0.5'})}).candidates[0].response_mix; b=s.solve(obs,decision_fingerprint='w',seed=1,range_beliefs={1:mk({'QcQd':'0.99','JhJd':'0.01'})}).candidates[0].response_mix; print({'effective_width_uniform':str(D(1)/(D('.5')**2+D('.5')**2)),'effective_width_concentrated':str(D(1)/(D('.99')**2+D('.01')**2)),'mix_uniform':a.to_dict(),'mix_concentrated':b.to_dict(),'equal':a==b})"
    result: "FAIL response width: effective widths 2.0 and 1.020199959... produced exactly equal fold/call/raise mixes because only the count of positive combo keys is used. R7-04 likelihoods are floored at 0.02, so public-action reweighting keeps the positive-support count and is invisible to this response feature."
    measured: true
  - command: >-
      py -3.13 -c "import runpy,time,statistics; from poker_coach.simulator import FastSolver; d=runpy.run_path('tests/test_fast_solver.py'); o=d['_observation'](active_seats=(0,1,2,3,4,5)); rs=d['_wide_ranges']((1,2,3,4,5)); ts=[]; ns=[]; ss=[]; s=FastSolver(); [(lambda i:(lambda t,r:(ts.append((time.perf_counter()-t)*1000),ns.append(r.sample_count),ss.append(r.status)))(time.perf_counter(),s.solve(o,decision_fingerprint=f'audit-{i}',range_beliefs=rs)))(i) for i in range(25)]; q=statistics.quantiles(ts,n=100,method='inclusive')[94]; print({'min_samples':min(ns),'max_samples':max(ns),'statuses':sorted(set(ss)),'p50_ms':round(statistics.median(ts),3),'p95_inclusive_ms':round(q,3),'reported_index_p95_ms':round(sorted(ts)[int(.95*(len(ts)-1))],3),'max_ms':round(max(ts),3)})"
    result: "PASS diagnostic: all 25 full-1,326-range standard runs were ready with exactly 256 samples; p50 86.831ms, correct inclusive p95 90.245ms, maximum 96.501ms. The current-head standard latency claim is independently credible on this host."
    measured: true
  - command: "git diff --check 11240329d8a20693a5bdef6e72fd45c2d29e5ae6..20c320e37013d703e81444c5878a2849f8b44ec8"
    result: "PASS: exit 0, no output."
    measured: true
  - command: "py -3.13 -m pytest backend -q"
    result: "Not run by contract; Controller owns batched/stage full backend gates."
    measured: false
  - command: "frontend test/type/build/E2E gates"
    result: "Not run; frontend is outside R7-05R scope and unchanged."
    measured: false
  - command: "git show --unified=80 --format=fuller f2b9632bf80545311be4ac30273f02aae87b3481 -- backend/poker_coach/simulator/fast_solver.py backend/tests/test_fast_solver.py"
    result: "Final re-review FAIL: R7-05R-P1-2 and R7-05R-P1-3 are closed; R7-05R-P1-1 remains because the bounded DFS avoids false uniform fallback but does not preserve the target product distribution conditioned on global card uniqueness."
    measured: true
  - command: "py -3.13 -m pytest -o addopts=\"\" backend/tests/test_fast_solver.py::test_joint_sampler_backtracks_from_dead_end_without_uniform_fallback backend/tests/test_fast_solver.py::test_raise_to_continuation_uses_asymmetric_opponent_call_increment backend/tests/test_fast_solver.py::test_response_mix_uses_blocker_conditioned_probability_mass_not_support_count backend/tests/test_fast_solver.py::test_private_poison_does_not_change_range_aware_result backend/tests/test_fast_solver.py::test_all_product_sizings_are_legal_and_preserve_cost_by_to_semantics backend/tests/test_fast_solver.py::test_response_mix_metrics_and_provenance_are_truthful_and_deterministic backend/tests/test_fast_solver.py::test_timeout_returns_partial_and_zero_samples_are_unavailable backend/tests/test_fast_solver.py::test_range_unavailable_honestly_falls_back_to_uniform_l1 backend/tests/test_fast_solver.py::test_fixed_river_range_matches_bruteforce_oracle_below_one_percentage_point backend/tests/test_fast_solver.py::test_standard_latency_is_monotonic_and_below_truthful_gate backend/tests/test_continuous_table_api.py::test_table_fast_solver_is_independent_and_rejects_a_stale_decision -q -s"
    result: "PASS: 11 passed in 1.83s; river equity MAE 0.000000pp; test-reported standard p50 35.334ms/p95 37.474ms. The new joint-support node proves no false fallback for one seed but does not assert sampling frequencies."
    measured: true
  - command: >-
      py -3.13 -c "from collections import Counter; import runpy; from poker_coach.simulator import FastSolver; d=runpy.run_path('tests/test_fast_solver.py'); o=d['_observation'](); rs=d['_ranges'](d['_belief'](1,{'QcQd':'0.99','JhJd':'0.01'}),d['_belief'](2,{'QcQs':'0.99','TcTd':'0.01'})); c=Counter(); s=FastSolver(); [(lambda x:c.update([( ''.join(x[5:7]), ''.join(x[7:9]) )]))(s.sample_trial(o,seed=i,range_beliefs=rs)) for i in range(5000)]; print({'observed':dict(c),'observed_pct':{str(k):round(v/50,2) for k,v in c.items()},'target_conditional_pct':{'QcQd/TcTd':round(.0099/.0199*100,2),'JhJd/QcQs':round(.0099/.0199*100,2),'JhJd/TcTd':round(.0001/.0199*100,2)}})"
    result: "FAIL weighting: observed QcQd/TcTd 79.74%, JhJd/QcQs 20.02%, JhJd/TcTd 0.24%; the product marginals conditioned on card compatibility require about 49.75%, 49.75%, 0.50%."
    measured: true
  - command: >-
      py -3.13 -c "import runpy,time,statistics; from poker_coach.simulator import FastSolver; d=runpy.run_path('tests/test_fast_solver.py'); o=d['_observation'](active_seats=(0,1,2,3,4,5)); rs=d['_wide_ranges']((1,2,3,4,5)); ts=[]; ns=[]; ss=[]; s=FastSolver(); [(lambda i:(lambda t,r:(ts.append((time.perf_counter()-t)*1000),ns.append(r.sample_count),ss.append(r.status)))(time.perf_counter(),s.solve(o,decision_fingerprint=f'review-final-{i}',range_beliefs=rs)))(i) for i in range(25)]; print({'min_samples':min(ns),'max_samples':max(ns),'statuses':sorted(set(ss)),'p50_ms':round(statistics.median(ts),3),'p95_inclusive_ms':round(statistics.quantiles(ts,n=100,method='inclusive')[94],3),'max_ms':round(max(ts),3)})"
    result: "PASS: all 25 full-1,326-range standard runs were ready with exactly 256 samples; p50 35.206ms, correct inclusive p95 38.163ms, maximum 45.228ms."
    measured: true
  - command: "git diff --check f2b9632bf80545311be4ac30273f02aae87b3481^..f2b9632bf80545311be4ac30273f02aae87b3481"
    result: "PASS: exit 0, no output for the Worker fix commit."
    measured: true
  - command: "git show --unified=100 --format=fuller 5c9023d6b0e3f32e6f897d30cd5810e151d9e0d5 -- backend/poker_coach/simulator/fast_solver.py backend/tests/test_fast_solver.py"
    result: "Final sampling review PASS: exact capped feasible-joint enumeration replaces first-feasible DFS; leaves carry the product of per-seat probabilities and are sampled from their normalized total. No remaining P0/P1 finding."
    measured: true
  - command: "py -3.13 -m pytest -o addopts=\"\" backend/tests/test_fast_solver.py::test_sparse_joint_sampler_preserves_conditional_product_weights backend/tests/test_fast_solver.py::test_joint_exact_cap_never_returns_first_feasible_range_sample backend/tests/test_fast_solver.py::test_joint_sampler_backtracks_from_dead_end_without_uniform_fallback backend/tests/test_fast_solver.py::test_raise_to_continuation_uses_asymmetric_opponent_call_increment backend/tests/test_fast_solver.py::test_response_mix_uses_blocker_conditioned_probability_mass_not_support_count backend/tests/test_fast_solver.py::test_private_poison_does_not_change_range_aware_result backend/tests/test_fast_solver.py::test_all_product_sizings_are_legal_and_preserve_cost_by_to_semantics backend/tests/test_fast_solver.py::test_timeout_returns_partial_and_zero_samples_are_unavailable backend/tests/test_fast_solver.py::test_range_unavailable_honestly_falls_back_to_uniform_l1 backend/tests/test_fast_solver.py::test_fixed_river_range_matches_bruteforce_oracle_below_one_percentage_point backend/tests/test_fast_solver.py::test_standard_latency_is_monotonic_and_below_truthful_gate backend/tests/test_continuous_table_api.py::test_table_fast_solver_is_independent_and_rejects_a_stale_decision -q -s"
    result: "PASS: 12 passed in 2.18s. The fixed 5,000-seed distribution observed 48.08%/51.32%/0.60% versus 49.75%/49.75%/0.50% targets; river equity MAE was 0.000000pp; test-reported standard p50 34.494ms/p95 35.920ms."
    measured: true
  - command: >-
      py -3.13 -c "import runpy,time,statistics; from poker_coach.simulator import FastSolver; d=runpy.run_path('tests/test_fast_solver.py'); o=d['_observation'](active_seats=(0,1,2,3,4,5)); rs=d['_wide_ranges']((1,2,3,4,5)); ts=[]; ns=[]; ss=[]; s=FastSolver(); [(lambda i:(lambda t,r:(ts.append((time.perf_counter()-t)*1000),ns.append(r.sample_count),ss.append(r.status)))(time.perf_counter(),s.solve(o,decision_fingerprint=f'review-pass-{i}',range_beliefs=rs)))(i) for i in range(25)]; print({'min_samples':min(ns),'max_samples':max(ns),'statuses':sorted(set(ss)),'p50_ms':round(statistics.median(ts),3),'p95_inclusive_ms':round(statistics.quantiles(ts,n=100,method='inclusive')[94],3),'max_ms':round(max(ts),3)})"
    result: "PASS: all 25 full-1,326-range standard runs were ready with exactly 256 samples; p50 34.309ms, correct inclusive p95 36.923ms, maximum 42.565ms."
    measured: true
  - command: "git diff --check 5c9023d6b0e3f32e6f897d30cd5810e151d9e0d5^..5c9023d6b0e3f32e6f897d30cd5810e151d9e0d5"
    result: "PASS: exit 0, no output for the final Worker sampling fix commit."
    measured: true
artifacts:
  - path: docs/orchestration/handoffs/R7-05R.md
    description: Independent R7-05 P0/P1 review verdict, findings, focused evidence, and minimal repair boundaries.
risks: []
decisions_needed: []
dependencies_unlocked:
  - R7-07
recommended_next:
  - task_id: R7-07
    goal: Integrate the accepted Fast Solver L1.5 output into the product while preserving decision identity, privacy, provenance, and non-blocking action flow.
    depends_on:
      - R7-05R
initial_review:
  reviewed_range: 11240329d8a20693a5bdef6e72fd45c2d29e5ae6..20c320e37013d703e81444c5878a2849f8b44ec8
  verdict: FAIL
  p0_findings: []
  p1_findings:
    - id: R7-05R-P1-1
      title: A sampled sequential dead end discards valid multiway joint range support and permanently falls back to uniform
      evidence:
        - "backend/poker_coach/simulator/fast_solver.py:204-214 treats any later-seat RuntimeError as proof that independent marginals have no compatible joint support, clears all range samples, resets the RNG, and disables range samplers for the rest of the solve."
        - "Measured three-seat counterexample alternates between range_weighted_public_beliefs and monte_carlo_uniform_opponents by seed even though at least one card-compatible assignment always exists globally."
      minimal_counterexample: "Seat 1 has 50% QcQd / 50% JhJd and seat 2 has 100% QcQs. If seat 1 first draws QcQd, seat 2 is blocked and the whole solve falls back; yet seat1=JhJd plus seat2=QcQs is valid joint support. Seeds 0,2,5,6,10 reproduced the false fallback with one iteration."
      impact: "A valid accepted Range V2 decision can silently become uniform-opponent L1 based on the deterministic draw path, so range-aware sampling is not reliably delivered and recommendations can materially change."
      minimal_fix_boundary: "In fast_solver.py only, distinguish a trial dead end from globally empty joint support. Retry/backtrack the bounded sequential assignment deterministically; use uniform fallback only after an exhaustive or otherwise sound proof that no compatible assignment exists. Add the exact overlapping-range regression and preserve bounded timeout behavior."
    - id: R7-05R-P1-2
      title: RAISE TO continuation EV uses Hero incremental cost as the opponent call amount
      evidence:
        - "backend/poker_coach/simulator/fast_solver.py:510-516 correctly computes Hero cost as raise-to minus Hero street commitment."
        - "backend/poker_coach/simulator/fast_solver.py:532-533 then uses that same chip_cost for both contributions in call_ev and for the synthetic re-raise branch, ignoring the opponent's different existing street commitment."
      minimal_counterexample: "Pot 300, Hero committed 50, opponent committed 100, Hero raises TO 300. Hero adds 250, but opponent calls only 200. At equity 1 the call-continuation net EV is 300+250+200-250=500; the implementation computes 300+2*250-250=550 before response mixing."
      impact: "Every facing-bet raise spot with unequal existing commitments overstates or understates continuation pots and can reorder min/mid/max raise recommendations, violating TO/BY/COST authority and EV chip units."
      minimal_fix_boundary: "In fast_solver.py's one-layer EV only, keep Hero incremental cost separate from each responder's incremental call amount derived from raise-to minus that active seat's street commitment; define the bounded re-raise sizing/cost explicitly instead of multiplying Hero cost. Add a raise-to EV regression with unequal commitments."
    - id: R7-05R-P1-3
      title: The response model ignores Range V2 probability concentration and therefore does not consume continuation-range width
      evidence:
        - "backend/poker_coach/simulator/fast_solver.py:576-588 counts only combos with probability >0 and divides that support count by a fixed 1,081 denominator."
        - "backend/poker_coach/ranges/likelihood.py:64-95 clips every public-action likelihood to at least 0.02, so accepted action updates reweight probabilities but do not reduce positive support; preflop/full-board blocker counts also differ from the fixed denominator."
        - "backend/poker_coach/simulator/table_insights.py:170-181 already defines the probability-sensitive effective width as 1/sum(p^2), but the solver does not use an equivalent calculation."
      minimal_counterexample: "The same two combos weighted 50/50 and 99/1 have effective widths 2.0 and about 1.0202. Both produce byte-identical fold/call/raise mixes because their positive combo count is two. In production Range V2 the same blindness applies to public-action likelihood updates."
      impact: "The claimed one-layer range-aware response branch is effectively insensitive to the opponent distribution learned from public actions; fold equity and bet/raise EV can therefore be materially wrong while provenance claims continuation-range use."
      minimal_fix_boundary: "In fast_solver.py only, derive a normalized per-seat probability-sensitive width/concentration from the blocker-conditioned combos (for example 1/sum(p^2), with the correct street-specific feasible-combo scale), aggregate it explicitly, and add a regression proving concentrated and diffuse beliefs change the response mix in the intended direction."
  verified_non_findings:
    - "All min/mid/max fold/check/call/bet BY/raise TO candidates are deduplicated and accepted by the authoritative LegalActionV1 boundary; Hero incremental candidate cost itself is correct."
    - "Range unavailable and genuinely unsupported sampling paths remain non-blocking and disclose uniform fallback source/range status; no private opponent card, RNG/deck state, terminal payout, or future-event dependency was found through the reviewed public-event seam."
    - "Response probabilities sum exactly to one and provenance/limitations do not claim GTO, Nash, CFR, or player/profile output."
    - "Decision seeding is stable, tier caps are nested, timeout returns partial/degraded or zero-sample unavailable, and the real-clock full-range standard benchmark passed independently on this host."
    - "The HU river equity oracle uses _best_hand_key_bruteforce rather than the production best_hand_key direct evaluator; measured MAE is 0.000000 percentage points on the focused fixture."
    - "The API validates the current hand/fingerprint and Hero actor before solving, passes only _public_stream plus Hero-visible blockers to Range V2, and returns the request decision identity so stale UI responses remain identifiable."
final_review:
  reviewed_fix_range: 20c320e37013d703e81444c5878a2849f8b44ec8..f2b9632bf80545311be4ac30273f02aae87b3481
  review_worktree_delivery_commit: 8e14b204ee9f68eed94fecd8ba81eb04aaca8618
  delivery_commit: f2b9632bf80545311be4ac30273f02aae87b3481
  verdict: FAIL
  p0_findings: []
  closed_p1:
    - id: R7-05R-P1-2
      result: PASS
      evidence: "Hero incremental cost is separated from each active opponent's stack-capped completion to the raise target; continuation pot is explicit, and the response-raise branch truthfully stops with Hero folding. The asymmetric 50/100 commitments regression passes."
    - id: R7-05R-P1-3
      result: PASS
      evidence: "Response strength now normalizes blocker-conditioned probability mass and uses effective width divided by the street-specific feasible combo count. Diffuse, concentrated, and blocker-conditioned regressions pass in the intended direction."
  remaining_p1:
    - id: R7-05R-P1-1
      title: Weighted DFS removes false uniform fallback but biases the card-compatible joint distribution
      evidence:
        - "backend/poker_coach/simulator/fast_solver.py:373-390 performs only 24 exact product-rejection attempts before switching to backtracking."
        - "backend/poker_coach/simulator/fast_solver.py:416-440 independently creates a size-biased order per seat and returns the first compatible DFS assignment; that first-feasible procedure is not a sample from the product of per-seat weights conditioned on global card uniqueness."
        - "Measured 5,000-seed diagnostic produced 79.74% / 20.02% / 0.24% for three compatible assignments whose normalized product weights are 49.75% / 49.75% / 0.50%."
      minimal_counterexample: "Seat 1 is 99% QcQd / 1% JhJd; seat 2 is 99% QcQs / 1% TcTd. Valid assignments have raw product weights 0.0099, 0.0099, and 0.0001. Because compatibility is only 1.99%, 24 rejection draws usually fail; DFS then tries seat1's QcQd first about 99% of the time and almost always returns QcQd/TcTd, distorting the required conditional distribution."
      impact: "High-overlap accepted ranges no longer disclose uniform fallback, but equity and action EV are still sampled from the wrong opponent distribution; the core per-seat-weight guarantee remains materially false."
      minimal_fix_boundary: "Change only the joint sampler/backtracking path to sample exactly or with a tested bounded-error approximation from the product weights conditioned on card uniqueness. Preserve deterministic seeds, bounded search/timeout honesty, and proven-empty uniform fallback. Add a deterministic many-seed frequency regression for this exact asymmetric-overlap fixture."
  verified_non_findings:
    - "Valid joint support no longer triggers monte_carlo_uniform_opponents in the direct dead-end regression, and proven-empty support still falls back honestly."
    - "RAISE TO opponent completion, continuation pot, response-raise stopping rule, candidate amount legality, and EV units pass the direct asymmetric and original legal-sizing regressions."
    - "Probability-sensitive blocker-conditioned response mix passes, remains normalized, and retains explicit non-GTO/non-Nash/non-profile provenance."
    - "Private poison, unavailable fallback, timeout status, stale fingerprint/Hero actor API behavior, independent river oracle, and fixed standard performance show no P0/P1 regression in the measured nodes."
final_sampling_review:
  reviewed_fix_range: f2b9632bf80545311be4ac30273f02aae87b3481..5c9023d6b0e3f32e6f897d30cd5810e151d9e0d5
  review_worktree_delivery_commit: 23b9a65d2d2c96b69245b6fd94de74cc3b5e1e0e
  delivery_commit: 5c9023d6b0e3f32e6f897d30cd5810e151d9e0d5
  verdict: PASS
  p0_findings: []
  p1_findings: []
  closed_p1:
    - id: R7-05R-P1-1
      result: PASS
      evidence:
        - "fast_solver.py:379-419 keeps the exact product-rejection fast path, then enumerates capped feasible assignments and samples from their normalized joint weights rather than returning the first feasible assignment."
        - "fast_solver.py:423-465 visits each deterministic MRV branch, multiplies every selected seat probability into joint_weight, records complete leaves only, and raises _JointSearchLimit before returning any truncated enumeration."
        - "The deterministic 5,000-seed gate observed 48.08%/51.32%/0.60% against 49.75%/49.75%/0.50%; the two dominant outcomes are within 1.67 percentage points, far below the prior approximately 30-point bias."
  distribution_gate:
    seeds: "0..4999 fixed and deterministic"
    samples: 5000
    expected_percentages:
      QcQd_TcTd: 49.75
      JhJd_QcQs: 49.75
      JhJd_TcTd: 0.50
    observed_percentages:
      QcQd_TcTd: 48.08
      JhJd_QcQs: 51.32
      JhJd_TcTd: 0.60
    tolerance_percentage_points: 3.0
    rationale: "At p approximately 0.4975 and n=5000, one standard error is about 0.71 percentage points; the deterministic 3-point gate is over four standard errors, robust to ordinary sampling variation and decisively rejects the former approximately 30-point bias. Exact support equality also requires all three feasible assignments to appear."
  cap_and_fallback:
    exact_caps: "20,000 visited states and 4,096 complete leaves"
    exhausted_with_zero_samples: "status=unavailable, source=range_weighted_public_beliefs, rangeStatus=ready, unavailableReason=range_joint_enumeration_cap_exhausted"
    exhausted_after_samples: "status=degraded with only earlier unbiased range samples and an explicit cap limitation"
    proven_empty: "completed zero-leaf enumeration alone permits honest monte_carlo_uniform_opponents fallback"
  verified_non_findings:
    - "No first-feasible or order-biased DFS remains; deterministic MRV affects enumeration order only, not leaf weights or the normalized draw."
    - "The new distribution and cap behavior do not read opponent-private cards, RNG/deck state, terminal payout, future events, or player/profile data."
    - "Asymmetric RAISE TO amount/pot/EV, probability-sensitive response mix, legal candidate acceptance, fallback/timeout, private poison, stale decision/Hero actor API, and independent river oracle regressions all pass."
    - "The full-1,326-range standard path completed all 256 samples in every run and independently met p50/p95 targets on this host."
```

R7-05R final sampling re-review verdict is PASS with no remaining P0/P1. R7-07 is unlocked for Controller acceptance and integration.
