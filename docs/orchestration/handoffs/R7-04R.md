# R7-04R independent narrow review handoff

```yaml
contract_version: handoff/v1
task_id: R7-04R
thread_id: /root/r7_04r_review
branch: codex/r7-04r
base_commit: 85e1b52ddd6300eddeb0976c8194d83102c4b508
head_commit: 9d34d84374cc7f3d02b1ebd617016de094c95264
reviewed_head_commit: 9d34d84374cc7f3d02b1ebd617016de094c95264
status: completed
scope:
  goal: Independently review the exact R7-04 Range V2 delivery for MVP-blocking P0/P1 privacy, blocker, public-semantics, DTO/provenance, cache-isolation, and performance-gate defects.
  included:
    - Review only 85e1b52ddd6300eddeb0976c8194d83102c4b508..9d34d84374cc7f3d02b1ebd617016de094c95264.
    - Inspect the directly changed Range implementation, direct Range/action contracts, R7 Range quality gate, R7-04 handoff, and the two directly named test files.
    - Report only P0/P1 findings with minimal counterexamples and repair boundaries.
  excluded:
    - Implementation changes, central ledger edits, full-suite tests, unrelated repository inspection, push, merge, and release acceptance.
changed_files:
  - docs/orchestration/handoffs/R7-04R.md
commits: []
quality_gates:
  - command: "git diff --unified=80 85e1b52ddd6300eddeb0976c8194d83102c4b508..9d34d84374cc7f3d02b1ebd617016de094c95264 -- backend/poker_coach/ranges/aggregation.py backend/poker_coach/ranges/event_beliefs.py backend/poker_coach/ranges/likelihood.py backend/poker_coach/ranges/seat_priors.py backend/poker_coach/simulator/table_insights.py"
    result: "FAIL review: three P1 findings recorded below; no P0 found."
    measured: true
  - command: "py -3.13 -m pytest -o addopts='' backend/tests/test_range_v2.py::test_v2_public_action_uses_size_street_position_spr_and_board_features backend/tests/test_range_v2.py::test_v2_same_public_prefix_ignores_rng_private_terminal_and_future_events backend/tests/test_range_v2.py::test_v2_summary_is_additive_stable_and_never_exposes_combo_detail backend/tests/test_event_beliefs.py::test_board_filters_every_seat_and_normalizes_with_hero_blockers backend/tests/test_event_beliefs.py::test_position_weighted_prior_and_public_actions_are_normalized_and_fold_inactive -q"
    result: "PASS: 5 passed in 0.90s; these existing nodes do not cover re-raise TO normalization, river draw suppression, or a representative multi-action latency prefix."
    measured: true
  - command: "git diff --check 85e1b52ddd6300eddeb0976c8194d83102c4b508..9d34d84374cc7f3d02b1ebd617016de094c95264"
    result: "PASS: exit 0, no output."
    measured: true
  - command: "py -3.13 -m pytest -o addopts='' backend/tests/test_range_v2.py::test_v2_six_max_current_decision_latency_gate -q -s"
    result: "Not rerun by R7-04R. R7-04 reported 1 passed with p50 20.748ms/p95 21.433ms, but that inherited result is measured:false here and its fixture-scope mismatch is finding P1-3."
    measured: false
artifacts:
  - path: docs/orchestration/handoffs/R7-04R.md
    description: Independent R7-04 P0/P1 review verdict, findings, focused evidence, and minimal repair boundaries.
risks:
  - "P1-1: RAISE amount_semantics=TO is bucketed as though it were incremental chips, so re-raises can receive the wrong public size bucket, likelihood, and change reason."
  - "P1-2: River made/draw features retain impossible one-card draw bonuses, so otherwise equivalent busted draws are incorrectly upweighted for river calls and other actions."
  - "P1-3: The latency gate uses an extremely short non-canonical prefix with only one public action after skipping preflop, so it does not establish the declared complete 6-max current-decision replay target."
decisions_needed: []
dependencies_unlocked: []
recommended_next:
  - task_id: R7-04
    goal: Reuse the original Worker to fix the three P1 findings and add focused regression/performance coverage, then return to R7-04R for narrow re-review.
    depends_on:
      - R7-04R
review:
  verdict: FAIL
  p0_findings: []
  p1_findings:
    - id: R7-04R-P1-1
      title: RAISE TO amounts are assigned a size bucket using total street commitment instead of chips added
      evidence:
        - "backend/poker_coach/ranges/likelihood.py:33-43 divides the raw action.amount by pot_before for every sized action."
        - "backend/poker_coach/ranges/event_beliefs.py:160-177 passes the raw TO amount into likelihood/change_reason, while lines 238-254 separately demonstrate the contract-correct TO conversion using amount minus street_contributions[actor]."
      minimal_counterexample: "With pot_before=2000, actor street contribution=500, and RAISE TO 1200, the public contract says only 700 chips are added (0.35 pot, small), but the current likelihood divides 1200/2000 (0.60, medium). The emitted change reason and range update therefore use the wrong bucket."
      impact: "Core Range V2 public action semantics are incorrect for re-raises and blind/previous-contribution raises; the error can materially invert the intended narrowing strength around bucket thresholds."
      minimal_fix_boundary: "In event_beliefs/likelihood only, derive a canonical incremental contribution from amount_semantics plus actor street contribution before size bucketing; retain the TO value separately for truthful observed metadata. Add a re-raise regression in test_range_v2.py or test_event_beliefs.py."
    - id: R7-04R-P1-2
      title: River likelihood treats impossible future-card draws as live strength
      evidence:
        - "backend/poker_coach/ranges/likelihood.py:119-160 computes flush/straight draw scores solely from combo+board and never suppresses them on Street.RIVER."
        - "backend/poker_coach/ranges/likelihood.py:103-110 adds both feature and draw bonuses to CALL, so the stale river draw is not merely descriptive metadata."
      minimal_counterexample: "On river board Kh-7h-2c-Jh-3d, Ah9s receives draw=2 from four hearts while As9s receives draw=0. No card remains to be dealt, yet a river CALL with Ah9s gets a large missed-flush-draw likelihood bonus over the same-rank As9s holding."
      impact: "River beliefs move in the wrong direction for busted draws, violating the made-hand/draw-aware public-action contract at a decision-critical street."
      minimal_fix_boundary: "Pass Street into the feature classifier/cache (or explicitly infer a complete five-card board) and force draw=0 on river before action likelihoods. Add a river call regression comparing same-rank suit variants."
    - id: R7-04R-P1-3
      title: Latency gate does not measure a representative complete current-decision public prefix
      evidence:
        - "backend/tests/test_range_v2.py:147-167 constructs only hand_started -> flop_dealt -> one bet, skipping every preflop action, then warms and repeats that identical three-event input."
        - "The implementation replays every action and performs an O(remaining-combos) likelihood pass per actor action, so one-action timing does not bound realistic multi-action decision prefixes."
      minimal_counterexample: "A normal 6-max flop decision necessarily follows public preflop actions and can include several flop actions; none of those replay costs exist in the measured fixture even though the handoff calls the result the complete current-decision update."
      impact: "The R7 median<25ms/p95<50ms quality gate remains unproven for the product path, so performance acceptance cannot be granted from the reported numbers."
      minimal_fix_boundary: "Change only the performance fixture/gate first: use a canonical representative or bounded-worst multi-action 6-max decision prefix while preserving the documented warm-cache policy, then rerun p50/p95. Optimize implementation only if that truthful gate fails."
  verified_non_findings:
    - "No P0/P1 evidence that opponent true hole cards, RNG seed, terminal payout, or later events alter the same explicitly bounded consumer prefix; private payloads are rejected at the consumer seam and filtered by Table Insights."
    - "Hero and public-board blockers are removed from combo state and exact Decimal normalization is preserved in the reviewed paths."
    - "The 169 DTO projection exposes aggregate cells/top classes only, adds fields without removing existing fields, and provenance remains explicitly heuristic rather than GTO/solver/profile."
    - "Reviewed caches include their content-affecting seat/position/blocker/board inputs or use guarded object identity; no P0/P1 cross-session/hand/seat pollution was found."
```

R7-04R is complete as a review task, but the reviewed R7-04 delivery fails acceptance until all three P1 items are repaired and narrowly re-reviewed.
