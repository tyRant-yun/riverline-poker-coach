```yaml
contract_version: handoff/v1
task_id: R9-05
thread_id: /root/r9_05_theory_explainer
branch: codex/r9-05-theory-explainer
base_commit: 2dbeb63508300ba462371927c0f46607b3ec4da4
head_commit: 69bdf8e3f11cf10ba423c81af4a2fdc5e156eff9
status: completed
scope:
  goal: Add one source-prioritized, permission-safe TheoryRecommendation path with Formula math as explanation only.
  included:
    - Immutable versioned TheoryRecommendation and TheoryEvidence DTOs with decision identity, provenance, coverage, policy fingerprint, normalized legal frequencies, legal sizes, assumptions, degradation and conditional oracle EV-loss fields.
    - Priority selection of verified preflop B artifact, caller-supplied projected-range HU river L2 B result, then honest Formula C or typed unsupported output.
    - Additive current-decision table endpoint POST /v1/tables/{session_id}/theory-recommendation with stale identity rejection and no live L2 range construction.
    - Focused contract/API tests for priority, normalization, illegal actions, oracle identity, privacy, stale requests and fallback.
  excluded:
    - Range prior/likelihood changes, PolicyArtifact data changes, L2 engine changes, frontend integration, ledger edits and full backend/frontend gates.
changed_files:
  - backend/poker_coach/api/app.py
  - backend/poker_coach/simulator/continuous_table.py
  - backend/poker_coach/theory/__init__.py
  - backend/poker_coach/theory/recommendation.py
  - backend/tests/test_theory_recommendation.py
commits:
  - sha: 69bdf8e3f11cf10ba423c81af4a2fdc5e156eff9
    subject: feat(theory): add unified recommendation explainer
quality_gates:
  - command: py -3.13 -m pytest backend/tests/test_theory_recommendation.py backend/tests/test_policy_artifact.py backend/tests/test_formula_advisor.py backend/tests/test_l2_solver.py backend/tests/test_decision_reconciliation.py backend/tests/test_continuous_table_api.py -q
    result: 66 passed; one pre-existing PokerKit fold warning
    measured: true
  - command: py -3.13 -m compileall -q backend/poker_coach/theory backend/poker_coach/simulator backend/poker_coach/api
    result: exit 0
    measured: true
  - command: py -3.13 -m poker_coach.theory --verify-corpus
    result: corpus_expectations_met=true (run from backend directory)
    measured: true
artifacts:
  - path: backend/poker_coach/theory/recommendation.py
    description: Consumer-facing unified source selector and immutable recommendation/explanation contract.
  - path: POST /v1/tables/{session_id}/theory-recommendation
    description: Additive current-Hero decision API endpoint.
risks:
  - Live continuous-table L2 is intentionally unavailable until R9-03 supplies a permission-safe projected range and matching range fingerprint; non-preflop live output is C Formula with an explicit degradation reason rather than a fabricated range.
  - EV loss is withheld unless the adapter is supplied a matching tree, range and utility fingerprint plus oracle data; the current preflop artifact has no same-oracle range identity.
decisions_needed: []
dependencies_unlocked:
  - R9-06
recommended_next:
  - task_id: R9-06
    goal: Render the unified recommendation/evidence/degradation DTO in the continuous-table training journey, consuming R9-03's same-source range contract when available.
    depends_on:
      - R9-03
      - R9-05
```
