# F1-07 handoff

```yaml
contract_version: handoff/v1
task_id: F1-07
thread_id: 019ff4e1-9866-7f93-837e-0dfc797034a8
branch: codex/f1-07-recovery-soak
base_commit: 8f3501c2c5ac8b35d7a45a716e6784bcdf5e8d67
head_commit: 548b8b9ea2d3511cbb61d8c453b46e3d496630e3
status: completed
scope:
  goal: Close the F1 authoritative-session recovery, replay, projection/outbox, 1,000-hand soak, and rollback exit gate.
  included:
    - Minimal SQLite durable GameSession snapshot repository with optimistic revision checks, idempotent retry, and terminal-event crash reconciliation.
    - Real-file mid-hand restart and cross-hand stack/button/hand-sequence recovery evidence with continuous/idempotent events and fixed-seed replay fingerprints.
    - Projection discard/rebuild, outbox dispatch accounting, and SQLite online backup/restore fingerprint-equivalence rehearsal.
    - Deterministic 1,000-hand fixed-seed 6-max soak covering normal completion, fold completion, all-in side pots, bust-out, and sparse stable active seats.
    - Operational documentation for backup, restore, and application rollback without destructive migration downgrade.
  excluded:
    - F2 bots/table API/frontend.
    - F3 Advisor/Range and F4 stats/review features.
    - PHH features, general multi-variant support, performance refactors, and P2/P3 cleanup.
    - Destructive migration downgrade execution.
changed_files:
  - backend/poker_coach/persistence/__init__.py
  - backend/poker_coach/persistence/game_session_schema.sql
  - backend/poker_coach/persistence/session_store.py
  - backend/tests/test_f1_recovery_exit.py
  - docs/ops-backup-restore.md
commits:
  - sha: 548b8b9ea2d3511cbb61d8c453b46e3d496630e3
    subject: "feat(simulator): close F1 recovery and soak gate"
quality_gates:
  - command: "py -3.13 -m pytest backend/tests/test_f1_recovery_exit.py -q"
    result: "2 passed before the soak slice was added; measured real-file restart, projection/outbox rebuild, and backup/restore."
    measured: true
  - command: "py -3.13 -m pytest backend/tests/test_f1_recovery_exit.py::test_fixed_seed_six_max_one_thousand_hand_soak_covers_f1_exit_invariants -q --durations=1"
    result: "1 passed; exactly 1,000 hands; soak test call duration 24.25s."
    measured: true
  - command: "py -3.13 -m pytest backend/tests/test_simulator_session.py backend/tests/test_game_orchestrator.py backend/tests/test_hand_event_store.py backend/tests/test_projection_recovery.py backend/tests/test_outbox_recovery.py backend/tests/test_postgres_recovery.py backend/tests/test_postgres_live.py -q"
    result: "76 passed, 10 skipped; skips were conditional live PostgreSQL tests because POKER_COACH_TEST_PG_URL was not configured."
    measured: true
  - command: "py -3.13 -m pytest backend/tests -q -ra"
    result: "480 passed, 10 skipped; all skips explicitly reported POKER_COACH_TEST_PG_URL not set; only existing PokerKit PHH extension-field warnings."
    measured: true
  - command: "py -3.13 -m compileall -q backend/poker_coach"
    result: "exit 0"
    measured: true
  - command: "py -3.13 -m pip check"
    result: "No broken requirements found."
    measured: true
  - command: "Live PostgreSQL recovery/backup exercise"
    result: "POKER_COACH_TEST_PG_URL was not configured; live PostgreSQL was not exercised."
    measured: false
  - command: "Frontend quality gates"
    result: "Not run because no frontend files or behavior were touched."
    measured: false
artifacts:
  - path: backend/tests/test_f1_recovery_exit.py
    description: Repeatable F1 recovery, projection/outbox, backup/restore, and 1,000-hand soak exit evidence.
  - path: docs/ops-backup-restore.md
    description: Operational backup/restore and non-destructive application rollback boundary.
risks:
  - Live PostgreSQL remains unmeasured because POKER_COACH_TEST_PG_URL was absent; no PostgreSQL GameSession snapshot adapter was added, so the new cross-hand durable seam is SQLite-only for the MVP.
  - SQLiteGameSessionStore and SQLiteHandEventStore must use the same database file for one recoverable deployment snapshot; the terminal event and session snapshot use separate transactions, with recover() intentionally closing that crash window.
  - The full backend gate emits existing PokerKit PHH warnings for Riverline extension fields; no PHH behavior was changed in this task.
decisions_needed: []
dependencies_unlocked:
  - F2 continuous-table vertical slice
recommended_next:
  - task_id: F2
    goal: Build the first user-experiencable continuous-table vertical slice on the closed F1 authoritative-session foundation.
    depends_on:
      - F1-07
```
