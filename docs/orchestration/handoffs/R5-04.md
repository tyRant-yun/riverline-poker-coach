# R5-04 MVP Experience Release Gate handoff

```yaml
contract_version: handoff/v1
task_id: R5-04
thread_id: 019ff53a-fe87-7353-b7cc-ac3288f0553e
branch: codex/r5-04-experience-gate
base_commit: ce3620edc1741f9ee5f1467fe909e40e72b373b2
head_commit: ce3620edc1741f9ee5f1467fe909e40e72b373b2
status: blocked
scope:
  goal: Run the one-time R5 MVP experience release gate and record only measured evidence.
  included:
    - Full host-Python backend pytest gate.
    - Minimal focused rerun of the first blocking failure for a reproducible counterexample.
  excluded:
    - Frontend, Playwright, provenance, and PR-check gates after the P1 persistence/money boundary failure.
    - Any product, test, rule, amount, recovery, ledger, remote, or main-branch changes.
changed_files: []
commits: []
quality_gates:
  - command: py -3.13 -m pytest -q
    result: 552 collected; 1 failed (post-bust sparse PHH regression), 10 skipped due existing unavailable live-service markers, and 541 passed before the gate stopped.
    measured: true
  - command: py -3.13 -m pytest backend/tests/test_phh_codec.py::test_authoritative_events_phh_round_trip_preserves_public_replay_facts --tb=short -q -k post-bust-sparse
    result: Reproduced 1 failure: GameCommandError expected actor seat 0, received seat 1.
    measured: true
  - command: npm test; npx tsc --noEmit; npm run build
    result: Not run; release gate stopped on the potential P1 backend failure.
    measured: false
  - command: R5-03 isolated SQLite/no-Redis launcher plus npx playwright test
    result: Not run; release gate stopped on the potential P1 backend failure.
    measured: false
  - command: py -3.13 tools/generate_license_provenance.py --check
    result: Not run; release gate stopped on the potential P1 backend failure.
    measured: false
  - command: GitHub PR #2 current check status/log summary
    result: Not read; release gate stopped before external PR-check inspection.
    measured: false
artifacts:
  - path: backend/tests/test_phh_codec.py
    description: Existing post-bust sparse-seat PHH round-trip regression that provides the minimum reproducible counterexample.
risks:
  - Potential P1 money/recovery regression: R5-02 auto-rebuy changes a zero-stack sparse seat into the current actor for a hand whose established PHH fixture expects the next active actor to be seat 1. The effect on intended post-bust lifecycle semantics requires independent review before any test or implementation change.
  - This is not classified as a test-only failure because the newly integrated auto-rebuy behavior is the likely boundary change; no safety or amount logic was relaxed.
  - No frontend, browser, provenance, or current PR-check evidence was produced in this interrupted gate.
decisions_needed:
  - Controller must decide the intended invariant for an already-busted seat at next-hand start: remain excluded from the sparse participant set, or receive auto-rebuy and re-enter the active actor rotation. Require an independent money/recovery review before repair.
dependencies_unlocked: []
recommended_next:
  - task_id: R5-02-POST-BUST-REBUY-P1
    goal: Independently determine the authoritative post-bust auto-rebuy and sparse-seat lifecycle invariant, then implement and regress only the approved behavior.
    depends_on:
      - R5-04
```
