# F1-01 Authoritative Session ownership/config handoff

```yaml
contract_version: handoff/v1
task_id: F1-01
thread_id: 019ff3b0-f805-7ff3-b510-ff5e778bcaf0
branch: codex/f1-01-authoritative-session
base_commit: 8dde576a0818be554a38e0420b025d4cd3bb51a7
head_commit: 141b1c57872353a8498f3aedba021e3726318315
status: completed
scope:
  goal: Define the authoritative GameSession ownership/configuration seam and minimal consecutive-hand lifecycle for the first Riverline product mode.
  included:
    - Stable session-scoped HandId generation, SessionId/HandId ownership validation, and immutable GameSession lifecycle transitions
    - Exact first-product NLHE configuration: six seats, 50/100 blinds, 10,000-chip (100BB) initial stacks, no ante, and no rake
    - Reusable contiguous 2-8 seat-topology validation, explicit sitting-out participation semantics, and button rotation over participating seats
    - Focused session ownership/configuration/lifecycle/stack non-mutation tests and a consumer-facing ownership document
  excluded:
    - Durable hand event storage, optimistic append, database migrations, or recovery
    - PokerKit dealing, blind posting, legal-action validation, betting, settlement, or stack updates
    - Bot, Advisor, frontend, ScenarioSpec bridge, or database work
changed_files:
  - backend/poker_coach/simulator/__init__.py
  - backend/poker_coach/simulator/session.py
  - backend/tests/test_simulator_session.py
  - docs/simulator-session-ownership-v1.md
commits:
  - sha: 141b1c57872353a8498f3aedba021e3726318315
    subject: "feat(simulator): add authoritative session seam"
quality_gates:
  - command: py -3.13 -m pytest -q backend/tests/test_simulator_session.py backend/tests/test_simulator_contracts.py backend/tests/test_simulator_replay.py backend/tests/test_bot_runtime.py backend/tests/test_evaluator_benchmark.py
    result: 27 passed
    measured: true
  - command: py -3.13 -m pytest -o addopts='' -q
    result: 381 passed, 8 skipped in 13.78s
    measured: true
  - command: py -3.13 -m compileall -q backend/poker_coach backend/tests
    result: exit 0
    measured: true
  - command: py -3.13 -m pip check
    result: No broken requirements found.
    measured: true
  - command: git diff --cached --check
    result: exit 0 before the F1-01 delivery commit
    measured: true
  - command: cd frontend && npx vitest run && npx tsc --noEmit && npm run build && npx playwright test
    result: Not run because F1-01 did not change frontend files.
    measured: false
artifacts:
  - path: backend/poker_coach/simulator/session.py
    description: Immutable GameSession aggregate, first-product config, 2-8 topology validation, and explicit hand lifecycle seam
  - path: backend/tests/test_simulator_session.py
    description: Focused evidence for identity, product configuration, topology, participation/button rotation, lifecycle, and stack ownership invariants
  - path: docs/simulator-session-ownership-v1.md
    description: Consumer-facing ownership, participation, configuration, and F1-03 boundary definition
risks:
  - The seam intentionally leaves stack mutation and all rule facts to the future PokerKit-backed F1-03 orchestrator; complete_active_hand only records lifecycle completion and rotates the button.
  - The 2-8 topology validator is reusable domain support, not a release or strategy-coverage claim for non-6-max tables.
  - WSL Ubuntu-24.04 can access this worktree but has no python3.13 executable; required Python validation used the repository's host py -3.13 runtime, consistent with the F0 evidence approach.
decisions_needed: []
dependencies_unlocked:
  - F1-02
recommended_next:
  - task_id: F1-02
    goal: Add the durable hand_events append port and PostgreSQL/SQLite adapters with event/sequence uniqueness and expected-sequence optimistic append.
    depends_on:
      - F1-01 controller acceptance
```

The lifecycle seam is intentionally rule-free: it captures an immutable opening
stack snapshot, prohibits overlapping active hands, and rotates only after an
explicit completion transition.  It does not manufacture PokerKit facts or move
chips.
