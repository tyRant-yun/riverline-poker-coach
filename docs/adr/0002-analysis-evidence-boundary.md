# 分析核心与规则核心的边界

状态：已接受

阶段 3 的数学、牌力、牌面和 Equity 分析使用 PokerKit 无关的项目内部值对象和确定性计算；PokerKit 仍是行动合法性、牌局状态和正式派奖的唯一权威。这样可以让分析在不泄漏上游类型的前提下支持精确枚举和受控模拟，同时避免把分析启发式误认为规则裁决。所有可供教学引用的定量结果都必须先进入带来源等级和版本的 `EvidenceBundle`。
