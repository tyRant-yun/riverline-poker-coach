# 工作准则

以下原则用于指导本项目中的所有工作；可根据任务风险、复杂度和用户明确指示动态调整轻重，但不得违背用户目标、约束或安全边界。

## 沟通与决策

- 保持简洁、直接。明确区分已验证的事实、合理推断和仍待确认的不确定事项。
- 解释复杂关系、流程或取舍时，优先采用最小且有助理解的可视化（如表格、流程图、时间线）；简单问题不为形式而可视化。
- 不随意打断用户提问。仅在缺少会显著改变结果、范围、成本或风险的关键决策时请求确认；其余情况下做出合理、可逆的假设并说明。
- 汇报对用户有价值的关键进展、结果、风险和验证结论，避免发送没有信息量的过程更新。

## 研究与执行

- 研究应以可靠、权威且尽可能一手的来源为依据；对时效性或高风险信息主动核实来源与日期。
- 始终围绕用户的目标、范围、约束和验收标准工作。发现冲突或范围扩张时，先说明影响并请求方向。
- 可在任务确实能被独立拆分、并行能明显缩短时间或提升质量时使用子 Agent；避免为简单、串行或高度耦合的工作进行无意义并行。
- 修改代码、配置或文档时保持克制：仅实施完成当前目标所必需的改动，不进行无关重构、格式化或清理。

## 环境与命令执行

- 当前权威 Python runtime 是宿主 CPython 3.13，Python 与 pytest 使用 `py -3.13` 直接执行；不要经 WSL 转发。WSL Ubuntu-24.04 只用于确实需要且已安装的 POSIX 工具。Codex Windows worktree 的 `.git` 指针可能包含宿主绝对路径；这类 worktree 的只读 Git 检查、提交、cherry-pick 与 worktree 控制使用宿主 Windows Git，避免在 WSL 中反复修正 Git 目录。不得借此触碰任务范围外文件或改变行尾。
- 确需从 Windows 宿主调用 WSL 时，使用 `wsl.exe -d Ubuntu-24.04 --cd <Linux 工作区路径> -- bash -lc '<command>'`；本工作区对应的 Linux 路径为 `/mnt/c/Users/Administrator/Documents/ChatGPT/德州扑克`。在同一次调用内不混用 PowerShell 与 POSIX 语法。
- 先确认实际运行时、依赖和工作目录，再执行测试或构建；不要假定 Windows 与 WSL 的 Python、Node、包缓存或环境变量相同。把连续的只读检查和测试合并为少量批处理调用，避免为每个微小命令重复启动 WSL。
- WSL 是默认命令环境，不改变安全与授权边界：文件修改仍优先使用补丁工具；网络、提权、破坏性操作及受限目录访问仍须遵循当前会话的权限规则。

## 质量与安全

- 将已有代码、数据和用户未要求改动的内容视为受保护资产。先检查上下文，避免覆盖、删除或回退既有工作；对破坏性操作先确认目标与授权。
- 以真实结果验证完成情况：运行相关测试、构建、检查或实际工作流，并如实报告结果与未验证的部分。不得仅凭表面检查或“看起来完成”宣布完成。

## Git 工作流

- 只要任务适合隔离开发，优先使用 Git worktree 管理改动，避免干扰用户当前工作区或并行任务；遵从项目现有分支与 worktree 约定。
- 将工作拆分为可独立验证的阶段。每个阶段完成并通过相应验证后，创建内容聚焦、信息明确的 Git 提交；不把无关改动混入提交。
- 提交前检查变更范围与工作区状态，确保不会意外纳入已有的用户改动、敏感信息或生成物。未经用户授权，不重写历史或执行破坏性 Git 操作。

## 任务回传与台账

- 所有独立 Codex 开发任务开始前必须阅读 `docs/orchestration/handoff-v1.md`；中央 `docs/orchestration/ledger.md` 只读取本任务状态行、直接依赖和“下一入口”，不重复加载历史验收记录。任务 prompt 必须列出本次真正需要的契约/测试文件，禁止默认全量阅读 master plan、全部 ADR 或历史 handoff。
- 结束前先提交范围内交付内容，再生成并提交 `docs/orchestration/handoffs/<task-id>.md`；最终回复必须按 handoff contract 结构化回传，且与仓库 handoff 文件一致。
- 每个质量门必须记录命令、结果和是否由本任务实测；不得把未运行或仅继承的基线声称为已通过。
- Worker 不直接修改 ledger 中央状态行，只新增自己的 handoff；主控验收/合并后单写 ledger，避免并行冲突。
