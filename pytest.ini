[pytest]
pythonpath = backend
testpaths = backend/tests
addopts = -q
markers =
    live: requires a real PostgreSQL instance (POKER_COACH_TEST_PG_URL)
filterwarnings =
    ignore::starlette.exceptions.StarletteDeprecationWarning
